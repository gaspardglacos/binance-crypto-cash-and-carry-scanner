# Core async + WebSocket clients
websockets>=12.0,<14.0
aiohttp>=3.9.0,<4.0

# Market data / risk-free benchmark
yfinance>=0.2.40,<0.3

# Test dependencies (kept here so a single `pip install -r requirements.txt`
# is enough to both run and verify the project).
pytest>=8.0,<9.0
pytest-asyncio>=0.23,<0.25
